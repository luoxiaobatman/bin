# session中去掉.gitconfig的filter
# git stash到固定name
# 添加filter标签
git config --add filter.gitignore.clean ""
git config --add filter.gitignore.smudge ""
git config --add filter.gitignore.clean "sed '/#gitignore$/'d"
git config --add filter.gitignore.smudge "cat"
